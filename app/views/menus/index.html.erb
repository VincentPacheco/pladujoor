<h1>Menus#index</h1>
<p>Find me in app/views/menus/index.html.erb</p>
<% @menus.each do |menu| %>
  <%= menu.id %>
  <%= menu.name %>
<% end %>

<%= simple_form_for ([ @restaurant, @menu ]) do |f| %>
  <%= f.input :name %>
  <%= f.submit %>
<% end %>
