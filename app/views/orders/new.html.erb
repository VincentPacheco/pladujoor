<!-- <h1>Orders#new</h1>

<%= form_for ([ @restaurant, @table, @order ]) do |f| %>
  <%= f.label :number %>
  <%= f.text_field :number %>
  <%= f.label :description %>
  <%= f.text_field :description %>
  <%= f.submit %>
<% end %> -->

<input type="search" class='searchbar' placeholder="Search">

<div class='top-page'>
  <h1><%= @restaurant.name%></h1>
</div>

<%= simple_form_for [@restaurant, @table, @order] do |f| %>
  <% @menu.categories.each do |category| %>
<div class='category-title'>
  <h2><%= category.name %></h2>
</div>

<div class="card-scroll">
  <div class='cards-container'>
      <% category.dishes.each do |dish| %>
    <div class='dishcard'>
      <%= image_tag "tomatesmozza.jpg", :class => "dishimg" %>

        <div class="textcontainer">
          <h2><%= dish.name %></h2>
          <div class='priceandbutton'>
            <h3><%= dish.price %>€</h3>
            <a class="btn-plus" href="#">+</a>
          </div>
      </div>
    </div>
        <% end %>
  </div>
</div>
<% end %>
<% end %>

<!-- <div class="container">
  <h1>Welcome to <%= @restaurant.name%> </h1>
    <div><%= @menu.name %> </div>
    <%#= render "shared/newcat", menu: @menu %>
    <%= simple_form_for [@restaurant, @table, @order] do |f| %>
      <% @menu.categories.each do |category| %>
        <h4><%= category.name %></h4>
        <% category.dishes.each do |dish| %>
          <div>name: <%= dish.name %>  price: <%= dish.price %> </div>
          <%= simple_fields_for :order_dishes do |od| %>
            <%= od.input dish.id, label: dish.name %>
          <% end %>
        <% end %>
      <% end %>
      <%= f.submit %>
    <% end %>

</div>
 -->
